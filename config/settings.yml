# config/settings.yml
plugins:
  skip_email_verification_enabled:
    default: true
    client: true
description: Enable to skip email verification during user registration. Users will be automatically activated.
